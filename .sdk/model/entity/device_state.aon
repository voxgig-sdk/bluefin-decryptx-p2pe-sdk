# Entity: device_state

main: kit: entity: device_state: {

  alias: field: {}

  fields: [
    {
      format: "int64"
      name: "id"
      req: false
      short: "Unique identifier for this Device state."
      type: "`$INTEGER`"
    }
    {
      name: "name"
      req: false
      short: "Descriptive name for this Device state."
      type: "`$STRING`"
    }
  ]

  id: {
    field: "id"
    name: "id"
  }

  name: "device_state"
  op: {
    list: {
      name: "list"
      points: [
        {
          method: "GET"
          orig: "/deviceStates"
          segments: [
            {
              lit: "deviceStates"
            }
          ]
          transform: {
            req: "`reqdata`"
            res: "`body.data`"
          }
        }
      ]
    }
  }



}
