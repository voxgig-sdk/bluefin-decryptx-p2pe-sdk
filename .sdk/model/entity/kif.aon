# Entity: kif

main: kit: entity: kif: {

  alias: field: {}

  fields: [
    {
      format: "int64"
      name: "id"
      req: false
      short: "This resource's unique identifier."
      type: "`$INTEGER`"
    }
    {
      name: "name"
      req: false
      short: "The KIF's name."
      type: "`$STRING`"
    }
  ]

  id: {
    field: "id"
    name: "id"
  }

  name: "kif"
  op: {
    list: {
      name: "list"
      points: [
        {
          method: "GET"
          orig: "/kifs"
          segments: [
            {
              lit: "kifs"
            }
          ]
          transform: {
            req: "`reqdata`"
            res: "`body.data`"
          }
        }
      ]
    }
  }



}
